/*The path address must be enclosed in quotation marks if it contains
  any spaces, such as a path address "C:\Program Files\Java".

  You must run the program from within its directory - you cannot use a
  path address as the java launcher requires a program name 
  not a file name.
  This was an intentional error exercise.*/

class Test 
{
     public static void main (String[] args)
     {
          String text = "success" ;
          System.out.println("Test " + text);

     }


}